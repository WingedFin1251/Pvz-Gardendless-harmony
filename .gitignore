/node_modules
/oh_modules
/local.properties
/.idea
**/build
/.hvigor
.cxx
/.clangd
/.clang-format
/.clang-tidy
**/.test
/.appanalyzer

# 签名密钥与口令文件（含 DevEco 的 p12 / cer / p7b 签名材料），禁止提交
*.jks
*.keystore
keystore.properties
*.p12
*.cer
*.p7b

# 本机 AI 工具配置（含绝对路径），不纳入版本管理
/.claude/

# 游戏负载（第三方 Cocos 构建产物）不纳入版本管理，仅保留两个手写壳层文件。
# 注意：必须用 /* 只匹配直接子项；写成 /** 或忽略整个 rawfile/ 目录会使下面的反选失效。
/entry/src/main/resources/rawfile/*
!/entry/src/main/resources/rawfile/index.html
!/entry/src/main/resources/rawfile/touchPatch.js

# 安卓参考实现（第三方工程，GPL-3.0，仅本地参考）
/gardendless-android*/

# 模块级 / 其它 IDE、系统与工具产出（根规则用 / 限定了目录，这里补齐通配）
**/local.properties
**/oh_modules
**/node_modules
/.preview/
*.iml
/.vscode/
.DS_Store
Thumbs.db
desktop.ini
*.log
*.hap
*.app
